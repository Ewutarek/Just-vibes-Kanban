
<div class="dropdown">
        <a href="/myBoards">My Boards</a>
         <div class="dropDownContent"> 
                {{#each (exportBoards)}}
                    <a href='/editBoard/{{this.id}}'>
                        <p>{{this.title}}</p>
                    </a>
                {{/each}}
            </div> 
    </div>

        <a href="/explore">Explore</a>
        
        Hello
        {{#getLoggedUser}}
            {{object}}
        {{/getLoggedUser}}
        

        <img class="avatar" src="{{#getLoggedAvatar}}
            {{avatar}}
        {{/getLoggedAvatar}}">
                <a href="/">Log out</a>